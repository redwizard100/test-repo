kind: pipeline
name: default

steps:
- name: hello
  image: alpine
  commands:
  - echo hello
  - echo world
---
kind: pipeline
name: tag

steps:
- name: hello
  image: alpine
  commands:
  - echo This is tagged commit

trigger:
  event:
  - tag
