plugins:
  davidheryanto/test-repo:
  - approve
  - assign
  - blunderbuss
  - help
  - hold
  - lgtm
  - lifecycle
  - trigger
  - wip
  # - lgtm
  # - config-updater

config_updater:
  maps:
    .prow/config.yaml:
      name: config
    .prow/plugins.yaml:
      name: plugins
    .prow/job-config.yaml:
      name: job-config
